VITE_API_BACKEND=aspnet

# Empty = relative URLs (/api/...) — correct for same-origin deployment
# where the backend serves the frontend from the same domain.
VITE_ASPNET_API_BASE_URL=
VITE_API_BASE_URL=
